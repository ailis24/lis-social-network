## 📝 **ПРОМТ ДЛЯ AGENT В REPLIТ:**

```javascript
Нужно сделать 2 важные вещи:

---

## 🎨 **1. TEAR EFFECT ПРИ УДАЛЕНИИ**

Добавь эффект "разрыва ткани" когда удаляю пост/сообщение/друга!

**Как должно работать:**
- Когда нажимаю кнопку "Удалить" на посте/элементе
- Элемент не исчезает сразу
- А плавно "рвётся" на части как ткань/бумага (как в видео Tearable UI)
- Разрывается пополам или на несколько кусков
- Куски разлетаются в стороны
- Исчезают через 1-1.5 секунды
- Только ПОСЛЕ этого удаляется из базы

**Техническая реализация:**
- Используй библиотеку для tear effect (найди подходящую npm библиотеку)
- ИЛИ сделай на CSS + JavaScript:
  * При клике "Удалить" → создаёшь 2-3 клона элемента
  * Применяешь CSS transform: rotate + translate
  * Добавляешь clip-path для эффекта "разрыва"
  * Анимация 1s ease-out
  * Потом удаляешь из DOM и базы

**Применить к:**
- Удалению постов в ленте
- Удалению комментариев
- Удалению друзей
- Удалению уведомлений

**Библиотеки которые можно использовать:**
- @react-spring/web (для физики)
- framer-motion (для анимаций)
- Или найди готовую tear effect библиотеку

Пример CSS:
```css
.tear-delete {
  animation: tearApart 1s ease-out forwards;
}

@keyframes tearApart {
  0% {
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
    transform: scale(1) rotate(0deg);
    opacity: 1;
  }
  50% {
    clip-path: polygon(0 0, 100% 0, 100% 50%, 0 50%);
    transform: scale(0.9) rotate(5deg);
    opacity: 0.8;
  }
  100% {
    clip-path: polygon(0 0, 100% 0, 100% 0%, 0 0%);
    transform: scale(0.5) rotate(15deg) translateY(-100px);
    opacity: 0;
  }
}
```

Сделай КРАСИВО и ПЛАВНО!

---

## 🔧 **2. ИСПРАВЬ СЛОМАННЫЕ ФУНКЦИИ**

У меня перестали работать важные функции — ВСЁ ИСПРАВЬ:

### **А. АУДИО И ВИДЕО ЗВОНКИ В ПРОФИЛЕ**

**Проблема:**
- В профиле пользователя пропали кнопки аудио и видео звонков
- Раньше были, теперь нет

**Что нужно:**
- Верни кнопки "📞 Аудиозвонок" и "📹 Видеозвонок" в профиль
- Они должны быть видны когда просматриваю чужой профиль
- При клике → открывается окно звонка (WebRTC)
- Логика должна работать:
  * Инициация звонка
  * Принятие/отклонение
  * Передача аудио/видео
  * Завершение звонка

**Проверь:**
- Компонент Profile.jsx
- Кнопки вызова
- WebRTC подключение
- Состояния звонка

---

### **Б. КОММЕНТАРИИ НЕ ОТКРЫВАЮТСЯ**

**Проблема:**
- Когда кликаю на пост → должны открыться комментарии
- Сейчас НЕ открываются
- Или кнопка "Комментарии" не работает

**Что нужно:**
- При клике на пост/кнопку "Комментарии" → открывается модальное окно или секция с комментариями
- Загружаются все комментарии к посту
- Можно написать новый комментарий
- Можно лайкнуть/удалить свой комментарий
- Работают ответы (reply) на комментарии

**Проверь:**
- Компонент Comments.jsx или Feed.jsx
- Функцию openComments/postId
- Загрузку комментариев из базы
- Форму добавления комментария
- Состояния loading/error

---

### **В. ДОБАВИТЬ В ДРУЗЬЯ**

**Проблема:**
- Кнопка "Добавить в друзья" не работает или пропала
- Запросы не отправляются

**Что нужно:**
- Кнопка "➕ Добавить в друзья" в профиле пользователя
- При клике → отправляется запрос на дружбу
- Статус: "pending" (ожидание)
- Пользователь получает уведомление
- Может принять или отклонить

**Логика:**
```javascript
// Отправка запроса:
const sendFriendRequest = async (userId) => {
  // Создать запись в friends collection
  // status: 'pending'
  // from: мой_id
  // to: userId
}

// Принятие:
const acceptFriend = async (requestId) => {
  // Обновить status: 'accepted'
  // Добавить в список друзей обоим
}

// Отклонение:
const declineFriend = async (requestId) => {
  // Удалить запрос или status: 'declined'
}
```

**Проверь:**
- Компонент Profile.jsx
- Кнопку AddFriend
- Firebase функции для друзей
- Уведомления о запросах

---

### **Г. УВЕДОМЛЕНИЯ С ПОДТВЕРЖДЕНИЕМ**

**Проблема:**
- Уведомления появляются
- Но НЕЛЬЗЯ подтвердить/отклонить
- Или не всплывают правильно

**Что нужно:**
- Когда приходит уведомление (запрос в друзья, приглашение)
- Должны быть кнопки:
  * "✅ Принять" (зелёная)
  * "❌ Отклонить" (красная/серая)
- При клике "Принять" → действие выполняется
- При клике "Отклонить" → уведомление исчезает
- Уведомление исчезает после действия

**UI уведомлений:**
```jsx
<div className="notification">
  <p>Пользователь хочет добавить вас в друзья</p>
  <div className="actions">
    <button onClick={accept} className="accept">✅ Принять</button>
    <button onClick={decline} className="decline">❌ Отклонить</button>
  </div>
</div>
```

**Проверь:**
- Компонент Notifications.jsx
- Список уведомлений
- Кнопки действий
- Обработку accept/decline
- Удаление уведомления после действия

---

## 🧪 **3. ПРОВЕРКА РАБОТОСПОСОБНОСТИ**

После исправлений **ПРОВЕРЬ ВСЁ:**

**Чек-лист:**
- ✅ Кнопки звонков в профиле → ВИДНЫ и РАБОТАЮТ
- ✅ Комментарии → ОТКРЫВАЮТСЯ, загружаются, можно писать
- ✅ Добавить в друзья → ОТПРАВЛЯЕТСЯ запрос
- ✅ Уведомления → ВСПЛЫВАЮТ с кнопками принять/отклонить
- ✅ Tear effect → РАБОТАЕТ при удалении
- ✅ Все кнопки → РЕАГИРУЮТ на клики
- ✅ Firebase → ДАННЫЕ сохраняются и загружаются
- ✅ Ошибки → НЕ ВЫЛЕТАЮТ в консоли

**Протестируй:**
1. Открой профиль друга → нажми "Позвонить" → должно работать
2. Открой пост → нажми "Комментарии" → должны открыться
3. Открой профиль не-друга → нажми "Добавить в друзья" → запрос уйдёт
4. Получи уведомление → должны быть кнопки ✅/❌ → работают
5. Удали пост → должен быть tear effect → пост исчезнет

---

## 🔍 **4. НАЙДИ И ИСПРАВЬ ОШИБКИ**

**Проверь логи в консоли:**
- Открой DevTools (F12)
- Посмотри Console → Errors
- Исправь все красные ошибки

**Проверь Firebase:**
- Открой Firebase Console
- Проверь collections:
  * posts
  * comments
  * friends
  * notifications
  * calls
- Убись что данные правильно сохраняются

**Проверь состояния:**
- useState для всех модальных окон
- useEffect для загрузки данных
- Обработку loading/error состояний

---

## 📦 **5. БИБЛИОТЕКИ**

Если нужно, установи:

```bash
# Для tear effect:
npm install framer-motion
npm install @react-spring/web

# Для звонков (если нет):
npm install simple-peer
npm install socket.io-client

# Для анимаций уведомлений:
npm install react-toastify
```

---

## ✅ **РЕЗУЛЬТАТ:**

После твоих исправлений:
1. **Tear effect** работает при удалении (красиво!)
2. **Звонки** работают (аудио и видео)
3. **Комментарии** открываются и работают
4. **Друзья** добавляются через запросы
5. **Уведомления** можно принять/отклонить
6. **ВСЁ работает** без ошибок!

**Сделай всё пошагово, проверь каждую функцию!** 🚀
```

---

**Скопируй этот промт и отправь агенту в Replit!** 

Агент исправит все баги и добавит крутой tear effect! 🎨✨